# Validation Report

The included validator checks that the portfolio contains its required architecture artifacts, validates key Terraform security/reliability properties by static assertion, reconciles the FinOps CSV model, and scans text files for placeholder markers.

Expected modeled FinOps values:
- baseline: **$28,000/month**
- savings backlog: **$5,790/month**
- optimized: **$22,210/month**
