# ADR-004 — OpenSearch for Near-Real-Time Exploration
**Status:** Accepted

OpenSearch serves low-latency event/session investigation. It is not the authoritative historical record. S3 remains durable truth so OpenSearch can be rebuilt after a failure.
